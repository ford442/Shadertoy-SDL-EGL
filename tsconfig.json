{
  "compilerOptions": {
    "target": "es2020", // Or a newer target if your browser supports it
    "module": "es2020", // Or another module system if you prefer
    "lib": ["dom", "es2020"], // Include necessary type definitions
    "outDir": "./js", // Output directory for compiled JavaScript
    "strict": true // Enable strict type checking
  }
}
